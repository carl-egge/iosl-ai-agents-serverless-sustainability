# GCP Authentication (optional - uses ADC by default)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Default project ID (can be overridden by CLI or config file)
# GCP_PROJECT_ID=your-gcp-project-id
