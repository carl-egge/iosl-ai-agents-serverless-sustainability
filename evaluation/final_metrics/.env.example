# Optional: Path to static configuration file
# STATIC_CONFIG_PATH=/path/to/static_config.json

# Optional: Path to function metadata file
# FUNCTION_METADATA_PATH=/path/to/function_metadata.json

# Default carbon intensity (gCO2/kWh)
# Can be overridden via --carbon-intensity CLI argument
# CARBON_INTENSITY_G_PER_KWH=400
